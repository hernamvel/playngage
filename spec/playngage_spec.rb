require 'spec_helper'

describe Playngage do
  it 'has a version number' do
    expect(Playngage::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
